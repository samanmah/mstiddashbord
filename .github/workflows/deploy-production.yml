# DEPRECATED — مسیر Deploy قدیمی حذف شد.
# تنها مسیر مجاز: .github/workflows/release.yml
#
# این فایل فقط برای جلوگیری از Deploy تصادفی روی push به main نگه داشته شده
# و هیچ Job اجرایی ندارد.

name: Deploy Production (deprecated)

on:
  workflow_dispatch:
    inputs:
      notice:
        description: 'Use release.yml instead — this workflow is intentionally empty'
        required: false
        type: string

jobs:
  deprecated:
    name: Redirect to release.yml
    runs-on: ubuntu-latest
    steps:
      - name: Fail with guidance
        run: |
          echo "deploy-production.yml is deprecated."
          echo "Use .github/workflows/release.yml:"
          echo "  - push to feature/advanced-project-control-wbs-gantt → Staging"
          echo "  - tag release-* → Staging + Production (environment approval)"
          echo "  - workflow_dispatch with deploy_production=true"
          exit 1
